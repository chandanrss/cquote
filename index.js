function todayQuote(string) {
    if (string === 'todayQuote') return "Push Yourself because no one else is going to do it for you";
    else return "Never Give up... Try Again.";
}
module.exports = todayQuote;